## Build

```bash
cd mgizapp/build
# cmake & make
bash install.sh 
```
### Run

Make sure you are under `mgizapp/build`.


The following script extract word alignment for parallel data:

```bash
bash run.sh 
```